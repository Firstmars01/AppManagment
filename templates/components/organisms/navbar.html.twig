<nav class="navbar navbar-expand-lg navbar-light bg-light mb-4">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav">
            {% if app.user %}
                <span class="navbar-text me-3">
                    Welcome {{ app.user.userIdentifier }}
                </span>
                <a class="btn btn-outline-danger" href="{{ path('app_logout') }}">Logout</a>
            {% else %}
                <a class="btn btn-outline-primary" href="{{ path('app_login') }}">Login</a>
            {% endif %}
        </div>
    </div>

    <div>
        <a class="btn btn-outline-secondary" href="{{ path('project_list') }}">Home</a>
        {% if app.user %}
            <a class="btn btn-outline-danger" href="{{ path('app_dashboard') }}">My Project</a>
        {% else %}

        {% endif %}
    </div>

    <div>
        <li class="nav-item dropdown">
                <ul class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdown-language">
                    <li><a class="dropdown-item" href="{{ path('project_list', {_locale: 'en'}) }}">English</a></li>
                    <li><a class="dropdown-item" href="{{ path('project_list', {_locale: 'fr'}) }}">Français</a></li>
                </ul>
        </li>
    </div>

    <p>{{ 'test' | trans}} </p>

    {% if app.user %}
        <a href="{{ path('app_crud_project_new') }}" class="btn btn-primary mb-3">
            Add a new project
        </a>
    {% endif %}


</nav>