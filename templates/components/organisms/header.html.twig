<!-- Project Header -->
<div class="d-flex justify-content-between align-items-center mb-4">
    <h1>{{ project.name }}</h1>
    <a href="{{ path('project_list') }}" class="btn btn-secondary">
        {{ 'project.back_to_list' | trans }}
    </a>
</div>
