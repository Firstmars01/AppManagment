<tbody>
{% for project in projects %}
    <tr>
        <td>
            <a href="{{ path('project_show', { slug: project.slug }) }}"> {{ project.name }}
            </a> </td>
        <td>{{ project.owner.name }} {{ project.owner.secondName }}</td>
        <td>{{ project.createdAt|date('d/m/Y') }}</td>
        <td>{{ project.progress }}%</td>
        <td>{{ project.requirementsCoverage }}%</td>
        <td>
            {% if project.theoreticalEndDate %}
                {{ project.theoreticalEndDate|date('d/m/Y') }}
            {% else %}
                <span class="text-muted">-</span>
            {% endif %}
        </td>

    </tr>
{% endfor %}
</tbody>